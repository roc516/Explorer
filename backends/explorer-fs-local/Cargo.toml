[package]
name = "explorer-fs-local"
version = "0.1.0"
edition = "2021"
description = "Local disk filesystem backend for Explorer"

[dependencies]
dirs = "6"
explorer-core = { path = "../../explorer-core" }
